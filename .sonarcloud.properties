# Capgo v2 checksum recovery requires raw RSA (.rsaEncryptionRaw) for Node privateEncrypt
# compatibility. Swift has no // NOSONAR under SonarCloud automatic analysis; exclude only
# the tiny raw-RSA shim so RSA.swift (unpadding, key import, cache) stays fully scanned.
sonar.exclusions=ios/Sources/CapacitorUpdaterPlugin/CapgoRawRsa.swift
